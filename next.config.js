/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
      },
    ],
  },
  // Enable static generation for SEO scale
  experimental: {
    staticGenerationRetryCount: 3,
  },
}

module.exports = nextConfig
